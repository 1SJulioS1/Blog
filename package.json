{
  "name": "blog",
  "version": "1.0.0",
  "description": "1. Create a Nodejs project using this command in cmd using `npm init -y`\r 2. Initialize git in project directory\r 3. Add node_modules to `.gitignore` file\r 4. Install dependencies:",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "juliosspz",
  "license": "ISC"
}
